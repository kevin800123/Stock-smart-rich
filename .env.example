# 全站登入保護（帳號+密碼兩者皆填才啟用；雲端強烈建議設定，本機開發可留空）。
# 設定後所有網頁與 /api 都需 HTTP Basic 帳密，瀏覽器會跳一次登入視窗、記住即可。
SPR_BASIC_USER=
SPR_BASIC_PASS=
GEMINI_API_KEY=
# LINE 官方帳號 Messaging API 的 Channel Access Token（LINE Developers Console 取得）。
# 設定後：SPR_SCHEDULE_TIME 更新完推「完整版」(broadcast 給全部好友)、平日 09:00-13:00 每 5 分掃突破。
# SPR_WEEKLY_PUSH_TIME：每週六此時間推「籌碼週報」(跨週變化＋AI)；日固定週六，僅時間可調。
# 註：16:00 的「盤後速報」已於 2026-08 移除，SPR_LINE_PUSH_TIME 不再有任何作用，設了也不會生效。
LINE_CHANNEL_ACCESS_TOKEN=
SPR_WEEKLY_PUSH_TIME=17:00
# 每日財經新聞 Telegram Bot（兩者皆設定才註冊 07:00／12:00／17:00／21:10 推播）。
# chat id 不要寫死在程式碼；部署時在 Zeabur 設為環境變數即可。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SPR_SCHEDULE_TIME=21:00
SPR_DB_PATH=data/spr.sqlite
SPR_DATA_DIR=Date
SPR_ENABLE_SCHEDULER=0
# 雲端部署(Zeabur)請另設 TZ=Asia/Taipei，並把 SPR_DB_PATH 指到持久化 Volume(例如 /data/spr.sqlite)
TZ=Asia/Taipei
